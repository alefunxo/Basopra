name: basopra_1
channels:
  - defaults
dependencies:
  - python=3.9
  - numpy>=2.0
  - pandas>=2.2
  - pytz>=2024.1
  - python-dateutil>=2.9
  - setuptools
  - pip
  - pip:
    - matplotlib>=3.9
    - pyomo>=6.8
    # Solver (pick one, not installed via this file):
    # - Windows: Gurobi (pip install gurobipy, requires a license)
    # - Linux: CPLEX (install via IBM's own installer, not pip-installable in general)
